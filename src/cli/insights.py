"""Deprecated wrapper (renamed placeholder).

This file should not be imported; kept only because Windows case-insensitive
filesystem may leave a stale reference. It immediately raises to prevent use.
"""
raise ImportError("Do not import cli.insights; use cli.extract_insights")
