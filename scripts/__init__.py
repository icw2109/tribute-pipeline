"""Helper package exposing script entrypoints for console_scripts.

This allows setuptools/pyproject console_entries to resolve functions,
while keeping each script runnable directly via `python scripts/<name>.py`.
"""
